# Copy to .env and adjust for this VPS. Everything here has a sane default
# baked into docker-compose.yml; nothing in this file is a secret any more,
# because this service no longer signs tokens — it only verifies them.

# Required. Postgres password for the compose-internal database.
POSTGRES_PASSWORD=

# Identity. This service no longer signs anything — it verifies tokens issued
# by id.build-with-deepak.com against that service's published JWKS, so there
# is deliberately no secret here to leak. See apps/api/src/auth/jwt-auth.guard.ts.
IDENTITY_ISSUER=https://id.build-with-deepak.com
IDENTITY_JWKS_URL=https://id.build-with-deepak.com/.well-known/jwks.json
IDENTITY_AUDIENCE=router.build-with-deepak.com
# Demo sessions expire after this. Cleanup of demo request history is
# separate (HISTORY_RETENTION_DAYS below).

# Ollama on the VPS, outside Docker.
OLLAMA_BASE_URL=http://host.docker.internal:11434
OLLAMA_MODEL=llama3:8b

# Cloud providers — OPTIONAL. Leave empty and the router runs local-only,
# with the decision panel showing cloud candidates as "not configured".
# Setting a key here makes that provider's models real routing candidates,
# and a public unattended demo will then spend real money on them — set a
# spend cap on the provider's own dashboard before enabling one here.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

MAX_PROMPT_CHARS=2000

# Demo request history older than this many days is deleted by the hourly
# in-app cron (seed rows are kept). Also runnable manually:
#   pnpm db:cleanup
HISTORY_RETENTION_DAYS=7

RATE_LIMIT_TTL_MS=60000
RATE_LIMIT_LIMIT=15
